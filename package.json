{
  "name": "@reactresume/workspace",
  "scripts": {
    "build": "npx tsc -p packages/types; npx tsc -p packages/components; npx tsc -p packages/jsonresume-parser; npx tsc -p packages/template",
    "serve": "npm run dev -w packages/server"
  },
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "workspaces": [ "packages/*" ]
}
